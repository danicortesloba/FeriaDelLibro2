<p>
  <strong>Name:</strong>
  <%= @membership.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @membership.description %>
</p>

<p>
  <strong>Price:</strong>
  <%= number_to_currency(@membership.price, precision:0) %>
</p>

<%= link_to 'Edit', edit_membership_path(@membership) %> |
<%= link_to 'Back', memberships_path %>
